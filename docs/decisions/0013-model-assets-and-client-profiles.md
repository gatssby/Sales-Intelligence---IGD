# ADR 0013 — Generic model assets and IGD client profile

## Status

Accepted for System One design.

## Decision

The model registry uses generic assets such as `sales-decision-v0.1`. IGD-specific taxonomy, products, rubrics, calibration and datasets remain Client Profile configuration. The model is never named an IGD model.
